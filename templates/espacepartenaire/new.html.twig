{% extends 'base.html.twig' %}

{% block title %}New Espace Partenaire{% endblock %}

{% block body %}
<div class="container py-5">

    <h1 class="mb-4">Create New Espacepartenaire</h1>

    <div class="row">
        <div class="col-md-8">
            {{ include('Espacepartenaire/_form.html.twig') }}
        </div>
    </div>

    <div class="mt-4">
        <a href="{{ path('app_espacepartenaire_show_accepted') }}" class="btn btn-secondary">Back to List</a>
    </div>
</div>
{% endblock %}
